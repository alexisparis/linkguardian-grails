
git add .
git add -u
git commit -m "$1"
git push origin master

grails clean

grails war
rm -Rf ../deployment/src/main/webapp/*
cp target/*.war ../deployment/src/main/webapp
cd ../deployment/src/main/webapp/
unzip *.war
rm *.war

cd ../../..

git add .
git add -u
git commit -m "$1"
git push origin master

cd ../Linkguardian
